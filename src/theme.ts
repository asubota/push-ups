import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#b73a72',
    },
    secondary: {
      main: '#35a67e',
    },
    background: {
      default: '#e0e0e0',
    },
  },
})

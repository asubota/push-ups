import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#673ab7',
    },
    secondary: {
      main: '#f9a825',
    },
    background: {
      default: '#e0e0e0',
    },
  },
})
